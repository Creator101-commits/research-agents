# Continue: Conventional dashboard + Blueprint + Excel parity (research-agents)

Repo: /Users/sreeharshak/Dev/research-agents, branch `feat/blueprint-excel-dashboard-parity` (main untouched; my old WIP is backed up at `refs/backup/wip-before-parity`). Read AGENTS.md first and follow it (stdlib only, no emojis in code/comments/commits/docs, conventional commits, keep README in sync, ask before irreversible actions, never force-push).

## Goals (precedence: Excel workbook > Blueprint.html > dashboard formulas, which only fill gaps and set layout)
1. The web UI must look and behave exactly like the "Conventional Farm" dashboard in `docs/parity/conventional_dashboard_target.html` (Dairy Bioeconomy Simulator v5.1).
2. Every formula must come from the Blueprint (`docs/parity/blueprint_extracted.txt`, summarized in `docs/parity/blueprint_formulas.md`).
3. Model output must match `Cdairy_mc_kk_current_PS_CM_Strategies.xlsm` (AIRAND strategy first).

Read before coding: `docs/parity/research-agents_parity_plan.md`, `docs/parity/dashboard_ui_spec.md`, `docs/parity/blueprint_formulas.md`.

## Already done (uncommitted on this branch - review with `git status` / `git diff`)
- Excel economics ported exactly: `dairy_abm/analysis/cdairy_economics.py` (verified vs all 5 strategies x 20 years; fixture `configs/reference_targets/cdairy_stats_mast.json`, built by `scripts/extract_cdairy_reference.py`).
- New herd lifecycle model `dairy_abm/herd_lifecycle.py` (reproduction, calving, culling, mastitis, heifers) calibrated to AIRAND year 15; params in `configs/calibration.json` groups `herd` and `cdairy_economics`.
- Blueprint formula fixes across agents (cow DMI NASEM Eq 2-1, THI, energy, manure N, water, environment, genetics NM$9 in `configs/genetics_nm9.json`, market USDA pricing, processor ERS coefficients).
- `dairy_abm/analysis/excel_parity.py` + CLI `python3 -m dairy_abm parity --seeds 1,2,3,4` + `excel_parity` block in reports. Last 3-seed result: 20 match / 6 close / 5 differ, profit -1.3%, total costs +0.1%.
- `dairy_abm/model.py` daily records now carry all fields the dashboard needs (compost_product_kg, digestate_kg, compost_n/p/k_kg, solar_generated_kwh, heat_value, wastewater_l, total_water_use_l, water_recovered_n/p/k_kg, soil_n_kg, pending_feed_credit_kg, pending_water_credit_l, whey_to_feed_l, waste_milk_to_feed_l, sludge_fertilizer_l, herd/loop economics lines).

## Step 0 - housekeeping
- Delete `configs/reference_targets/cdairy_airand_milk_calibration.json` (obsolete; ask me for delete permission if needed).
- Run `python3 scripts/extract_cdairy_reference.py Cdairy_mc_kk_current_PS_CM_Strategies.xlsm /tmp/fixture.json` and confirm it equals `configs/reference_targets/cdairy_stats_mast.json`.
- Run `python3 -B -m unittest discover -s tests -p 'test_*.py'`. Expected now: 254 tests, 4 known failures (test_dashboard_cows FCR division by zero with dry cows; test_dashboard_economics `labor_cost` KeyError - labor cost was removed; test_dashboard_environment monthly sustainability is now None by design unless scenario weights are set; test_webapp reference scenario days 1095 != 365). Fix them as part of step 1 so the tests reflect the new semantics.

## Step 1 - rebuild the web UI as the exact Conventional dashboard (main remaining work)
Do it yourself; do not delegate to subagents.
- Copy the target's CSS, markup and rendering JS verbatim into `web/index.html`, `web/styles/dashboard.css`, `web/js/dashboard.js`. Conventional only: no farm landing screen (open `#mainApp` directly), farm pill static. UI text/icons stay exactly as in the target (that is my explicit requirement, so the emoji icons in UI content are allowed); your own comments stay emoji-free.
- Vendor Chart.js 4.4.2 into `web/vendor/chart.umd.js` plus its LICENSE (approved); no CDN script. Keep the Google Fonts link.
- Remove the in-browser engine (makePRNG, gauss, woodNorm, seasonal/drawDaily helpers, Cow, HerdModel, simulateDay, runSim). Replace it with backend calls; keep every renderer (renderKPIs, animateKPIs, updateRevSummary, buildOverviewCharts, buildAllCharts, buildLoops, comparison modes A/B, radar, group avg, buildEquipROI + EQUIPMENT_CATALOGUE, ranking table/charts, param forms, navigate, theme toggle).
- New `dairy_abm/dashboard_conventional.py` that runs `DairyFarmModel` (baseline scenario; defaults 100 cows, 5 years, seed 42, all 4 loops on) and returns exactly the shape the target JS consumes: `{series, avg, totals, days, n, cfg, herdSummary}`. Key mapping from daily records: milk<-milk_l, manure<-manure_kg, feedDemand<-dmi_kg, waterBase<-total_water_use_l, netFeed<-dmi_kg minus l1/l4 feed offsets (or purchased_feed_kg_dm, check which matches the ledger), netWater<-net_water_l, whey<-whey_l, wastewater<-wastewater_l, compost<-compost_product_kg, nRec/pRec/kRec<-compost_n/p/k_kg, wN/wP/wK<-water_recovered_*, nRecTotal etc = sums, biogasM3<-biogas_volume_m3, elec<-electricity_generated_kwh, heat<-heat_generated_mj/3.6 (kWh), solar<-solar_generated_kwh, totalElec=elec+solar, recycledIrrig<-recycled_irrigation_l, freshOffset<-l2_water_offset_l, waterCredit<-water_loop_offset_l, organicFert=compost+digestate+sludge fertilizer, digestate<-digestate_kg, feedOffset<-l1_feed_offset_kg, l4FeedOffset<-l4_feed_offset_kg, wheyToFeed<-whey_to_feed_l, wasteToFeed<-waste_milk_to_feed_l, feedReturn<-l4 feed credit, grossGHG<-gross_kg_co2e, netGHG<-net_kg_co2e, avoidedGrid<-energy_offset_kg_co2e, biogasDispl<-fertilizer_offset_kg_co2e, milkRev<-milk_revenue, energyVal<-energy_value, heatVal<-heat_value, compostRev<-compost_revenue, totalRevenue<-total_revenue, feedCost<-feed_cost, waterCost<-water_cost, profit<-profit, netFCR/grossFCR per kg milk (None-safe), sludgeFertilizer, nutrientPool<-soil_n_kg, byproductFeedCredit<-pending_feed_credit_kg, waterRecycleCredit<-pending_water_credit_l, energyCredit 0. Add the same aliases runSim adds (avg.feed, biogas, fcr, profitability, water, wheyFoods; totals.feedCostTotal, waterCostTotal, circularOutput; series aliases). Verify each mapping against the agent code, do not guess units.
- `herdSummary` for Cow Ranking: per adult cow from cow dict histories (milk_history, dmi_history, manure_history, health_history, ch4_history, reproduction_history, body_weight_kg, parity, peak_factor, removal_reason/alive, id prefix `cow-` = founder): id, parity, bw, peakFactor, milk (avg L per milking day), feed, water (allocate herd drinking water per cow the same way the water agent does), manure, fcr (feed/milk kg), milkRev (avg milk L x run average revenue per L), ghg (avg CH4 x environment.ch4_gwp100), daysActive, daysMilking, sickDays, calvings, cullReason ('active' or removal reason), isFounder.
- Endpoints in `webapp.py`: `POST /api/conventional/run`, `POST /api/conventional/compare` (the 16 loop combinations, run in parallel with ProcessPoolExecutor), `GET /api/conventional/params` (target cfg keys -> scenario/calibration overrides, with backend default values). Controls with no Python equivalent stay visible but disabled with tooltip "Not used by the Python model". Keep all existing API routes working.
- Truthful labels: Net Profit subtitle "revenue - all costs"; add an "Other herd costs/revenue (workbook)" row in the Input Costs card so the card sums to profit. Add Export (zip/CSV) to the top bar, an "Excel parity" card on the Comparison page (from `excel_parity`), and a collapsible "Model details" card on Parameters.
- Replace/update dashboard and webapp tests for the new payload and routes.
- Visually verify with Playwright/Chromium: screenshot every page of the target HTML and of the app (light and dark) and compare side by side until they match.

## Step 2 - verification and docs
- `python3 -m dairy_abm parity --seeds 1,2,3,4,5,6,7,8` and write the results into `docs/parity/parity_report.md` (honest: list what matches, what differs and why - e.g. age of dam is definitional, breeding cost -6.7%, workbook breeding-cost cell bug AA471).
- Full test suite green. Update README and docs (calibration review, model explanation) for every change.
- Do not commit until I review; then propose conventional commits split by area (economics, herd model, blueprint fixes, UI, docs).
